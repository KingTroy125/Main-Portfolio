---
// Remove the direct image import since we'll use public folder
---

<section id="about" class="py-24 border-t border-[#ffffff10] section-transition">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-lg text-[var(--sec)] mb-2">About me</h2>
    <h3 class="text-4xl md:text-5xl font-medium text-[var(--white)] mb-8">Who I am</h3>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
      <div class="space-y-6 fade-in-up">
        <div class="relative animate-on-scroll fade-right">
          <div class="relative group cursor-pointer" id="image-container">
            <img
              id="profile-image"
              src="/images/developer1.jpg"
              alt="Developer Portrait"
              class="rounded-2xl border-2 border-[var(--white-icon-tr)] w-full h-auto object-cover transition-opacity duration-500"
              width={500}
              height={600}
            />
            <img
              id="profile-image-alt"
              src="/images/developer.jpg"
              alt="Developer Portrait Alternative"
              class="rounded-2xl border-2 border-[var(--white-icon-tr)] w-full h-auto object-cover absolute top-0 left-0 opacity-0 transition-opacity duration-500"
              width={500}
              height={600}
            />
          </div>
          <div class="absolute -bottom-6 -right-6 p-4 bg-[#1414149c] rounded-2xl border border-[var(--white-icon-tr)] backdrop-blur-sm animate-float">
            <p class="text-[var(--white)] font-medium">Tristan Hendricks </p>
            <p class="text-[var(--white-icon)] text-sm">Software Developer</p>
          </div>
        </div>
      </div>
      
      <div class="space-y-6">
        <p class="text-[var(--white-icon)] text-lg leading-relaxed animate-on-scroll fade-left">
          I'm a dedicated Software Developer with a passion for crafting exceptional digital experiences. Specializing in frontend development, I turn complex challenges into seamless, intuitive solutions.
        </p>
        <p class="text-[var(--white-icon)] text-lg leading-relaxed animate-on-scroll fade-left" style="--delay: 0.2s">
          With a strong foundation in modern web technologies, I focus on writing clean, efficient code and designing user-friendly interfaces. My goal is to create impactful, innovative experiences that resonate with users. Let's build something extraordinary together!
        </p>
        <div class="flex gap-4 pt-4 animate-on-scroll fade-left" style="--delay: 0.4s">
          <a 
            href="/images/CV/Tristan_Hendricks_CV.pdf"
            download="Tristan_Hendricks_CV.pdf"
            class="px-6 py-3 bg-[var(--sec)] text-[var(--background)] rounded-xl hover:opacity-90 transition-opacity font-medium"
          >
            Download CV
          </a>
          <a 
            href="#contact"
            class="px-6 py-3 border border-[var(--white-icon-tr)] text-[var(--white)] rounded-xl hover:bg-[var(--white-icon-tr)] transition-all group"
          >
            <span class="flex items-center gap-2">
              Get in touch
              <svg xmlns="http://www.w3.org/2000/svg" class="size-5 group-hover:translate-x-1 transition-transform" viewBox="0 0 24 24" fill="currentColor">
                <path d="M16.1716 10.9999L10.8076 5.63589L12.2218 4.22168L20 11.9999L12.2218 19.778L10.8076 18.3638L16.1716 12.9999H4V10.9999H16.1716Z"></path>
              </svg>
            </span>
          </a>
        </div>

        <div class="grid grid-cols-2 gap-6 pt-8">
          <div class="p-6 bg-[#1414149c] rounded-2xl border border-[var(--white-icon-tr)] animate-on-scroll fade-up" style="--delay: 0.6s">
            <h4 class="text-4xl font-bold text-[var(--white)] mb-2">3+</h4>
            <p class="text-[var(--white-icon)]">Years of Experience</p>
          </div>
          <div class="p-6 bg-[#1414149c] rounded-2xl border border-[var(--white-icon-tr)] animate-on-scroll fade-up" style="--delay: 0.8s">
            <h4 class="text-4xl font-bold text-[var(--white)] mb-2">20+</h4>
            <p class="text-[var(--white-icon)]">Projects Completed</p>
          </div>
        </div>
      </div>
    </div>

    <div class="mt-16 pt-16 border-t border-[#ffffff10]">
      <h2 class="text-lg text-[var(--sec)] mb-2">Work History</h2>
      <h3 class="text-4xl md:text-5xl font-medium text-[var(--white)] mb-8">Experience</h3>
      
      <div class="space-y-8">
        <div class="flex items-start gap-4 p-6 bg-[#1414149c] rounded-2xl border border-[var(--white-icon-tr)] hover:border-[var(--sec)] transition-all duration-300 group">
          <img 
            src="/images/companies/theforage_logo.jpg" 
            alt="Forage" 
            class="w-12 h-12 rounded-lg object-cover"
          />
          <div class="flex-1">
            <div class="flex justify-between items-start w-full">
              <div>
                <h5 class="text-[var(--white)] font-medium group-hover:text-[var(--sec)] transition-colors">Software Engineer</h5>
                <p class="text-[var(--white-icon)]">@Electronic Arts Software Engineering</p>
              </div>
              <span class="text-[var(--white-icon)] text-sm ml-4 whitespace-nowrap shrink-0">Jan — Present</span>
            </div>
          </div>
        </div>

        <div class="flex items-start gap-4 p-6 bg-[#1414149c] rounded-2xl border border-[var(--white-icon-tr)] hover:border-[var(--sec)] transition-all duration-300 group">
          <img src="/images/companies/uwc_logo.jpg" alt="Hackathon Participant" class="w-12 h-12 rounded-lg object-cover" />
          <div class="flex-1">
            <div class="flex justify-between items-start w-full">
              <div>
                <h5 class="text-[var(--white)] font-medium group-hover:text-[var(--sec)] transition-colors">UWC Samsung Future Innvotion Lab</h5>
                <p class="text-[var(--white-icon)]">@Hackathon Participant</p>
              </div>
              <span class="text-[var(--white-icon)] text-sm ml-4 whitespace-nowrap shrink-0">Feb 2025</span>
            </div>
          </div>
        </div>

        <div class="flex items-start gap-4 p-6 bg-[#1414149c] rounded-2xl border border-[var(--white-icon-tr)] hover:border-[var(--sec)] transition-all duration-300 group">
          <img src="/images/companies/freelancer_com_logo.jpg" alt="Social3" class="w-12 h-12 rounded-lg object-cover" />
          <div class="flex-1">
            <div class="flex justify-between items-start w-full">
              <div>
                <h5 class="text-[var(--white)] font-medium group-hover:text-[var(--sec)] transition-colors">Freelancer - Full Stack Developer</h5>
                <p class="text-[var(--white-icon)]">@Full Stack Developer</p>
              </div>
              <span class="text-[var(--white-icon)] text-sm ml-4 whitespace-nowrap shrink-0">Sep 2024</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }

  .animate-float {
    animation: float 4s ease-in-out infinite;
  }

  .animate-on-scroll {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .animate-on-scroll.show {
    opacity: 1;
    transform: translateY(0);
  }

  .fade-up { transform: translateY(20px); }
  .fade-left { transform: translateX(20px); }
  .fade-right { transform: translateX(-20px); }

  .animate-on-scroll {
    transition-delay: var(--delay, 0s);
  }

  #image-container {
    transition: transform 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  #image-container:hover {
    cursor: pointer;
  }

  #profile-image, #profile-image-alt {
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
  }
</style>

<script>
  // Create a more robust intersection observer
  const createObserver = () => {
    const options = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('show');
          // Optional: Unobserve after animation
          observer.unobserve(entry.target);
        }
      });
    }, options);

    document.querySelectorAll('.animate-on-scroll').forEach(
      (element) => observer.observe(element)
    );
  };

  // Run after DOM is loaded
  document.addEventListener('DOMContentLoaded', createObserver);

  // Image switcher functionality
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('image-container');
    const mainImage = document.getElementById('profile-image');
    const altImage = document.getElementById('profile-image-alt');
    let isMainImage = true;

    if (container && mainImage && altImage) {
      container.addEventListener('click', () => {
        if (isMainImage) {
          mainImage.style.opacity = '0';
          altImage.style.opacity = '1';
        } else {
          mainImage.style.opacity = '1';
          altImage.style.opacity = '0';
        }
        isMainImage = !isMainImage;
      });

      // Optional: Add hover effect hint
      container.addEventListener('mouseenter', () => {
        container.style.transform = 'scale(1.02)';
      });

      container.addEventListener('mouseleave', () => {
        container.style.transform = 'scale(1)';
      });
    }
  });
</script>